package org.firstinspires.ftc.teamcode.robotSubSystems.Conveyor;

public enum ConveyorState {
    TRANSPORT, STOP, BACKWARDS, OVERRIDE, INTAKE //backwards refers to when we don't want to shoot so we rotate the conveyor motor on the opposite direction.
}
